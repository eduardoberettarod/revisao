import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({

    input: {
        borderColor: '#0D0D0D',
        borderWidth: 1,
        borderRadius: 10,
        minWidth: 320,
        padding: 14,
        fontSize: 16
    }

})