import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    button: {
        backgroundColor: 'black',
        borderRadius: 100,
        padding: 14,
    },
    textButton: {
        fontSize: 24,
        color: '#f2f2f2',
        fontWeight: 700,
        textAlign: 'center'
    }
})